## Codebase Architecture Rules
- ALWAYS check `graphify-out/graph.json` or `GRAPH_REPORT.md` first before exploring the repository.
- Use the graph to trace dependencies (`/graphify path`) instead of using broad greps or opening multiple unedited files.
- Run `/graphify .` to re-index the map after adding new components or refactoring core architecture.
